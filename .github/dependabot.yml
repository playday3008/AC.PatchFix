version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  # FetchContent deps (safetyhook, Hooking.Patterns, mINI, spdlog) are pinned
  # in cmake/deps/*.cmake and tracked manually — Dependabot cannot monitor them.
